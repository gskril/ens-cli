{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "repo_root=$(git rev-parse --show-toplevel 2>/dev/null) && cd \"$repo_root\" && bun run format 2>/dev/null || true",
            "statusMessage": "Formatting with prettier..."
          }
        ]
      }
    ]
  }
}
