{
  "name": "@ensdomains/cli",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "ens": "dist/ens.mjs"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/ens.mjs --target node --minify",
    "format": "prettier --write ."
  },
  "files": ["dist"],
  "devDependencies": {
    "@types/bun": "latest",
    "incur": "^0.3.13",
    "prettier": "^3.8.1",
    "viem": "^2.47.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
