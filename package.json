{
  "name": "@ensdomains/cli",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "ens": "dist/ens.mjs"
  },
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/ens.mjs --target node --minify",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "incur": "^0.3.13",
    "viem": "^2.47.6"
  }
}
